/**
 * Bootstrap file for Laravel.
 * Axios è stato rimosso perché non utilizzato nell'app (SPA senza chiamate API).
 * Se in futuro servono API, decommentare:
 *
 * import axios from 'axios';
 * window.axios = axios;
 * window.axios.defaults.headers.common['X-Requested-With'] = 'XMLHttpRequest';
 */
